# Unispin — agent context

This repo is **code only** (RTL, asm, PCB). Lab knowledge lives in a separate vault.

## Wiki knowledge base

**Path:** `pennqubit-knowledge` repo — https://github.com/JiwaniZakir/pennqubit-knowledge

When you need context not in this repo (spin qubit physics, AXI, RFDC, platform architecture):

1. Read `wiki/hot.md` in that vault
2. Read `wiki/index.md`
3. Read `04-unispin-platform/` notes as needed
4. Return here for implementation

Do not duplicate long explanations in commits — link to vault pages.

## Fork workflow

```bash
git fetch upstream
git merge upstream/main
```

Upstream: https://github.com/ShizeChe/unispin
